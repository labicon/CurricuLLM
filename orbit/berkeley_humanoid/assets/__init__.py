"""Package containing assets implementations for various robotic environments."""

import os
import toml

# Conveniences to other module directories via relative paths
ORBIT_ASSET_DIR = os.path.abspath(os.path.dirname(__file__))
